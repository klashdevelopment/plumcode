META PRODUCTION; >> Set the meta environment to PRODUCTION.

>> Define a variable that stores the text we will print.
VAR var = "The GUI Example is now running.";
>> Print the var.
PRINT *var*;
>> Make our gui center horizontally and vertically using Plumcode's Config system (SET).
SET GuiCenter="true";
>> Create the HTML GUI using variables inside (Javascript/interactive functions are not allowed).
GUI "Title" "<html><h1>Simple GUI</h1><h2 id='result'>Result of 1+1: $(1+1)</h2></html>";







